<% team = @team || @team %>
<% context ||= team %>
<% collection ||= :demographics %>
<% hide_actions ||= false %>
<% hide_back ||= false %>

<% with_attribute_settings object: demographic do %>
  <tr data-id="<%= demographic.id %>">
    <%= render "shared/tables/checkbox", object: demographic %>
    <td><%= render 'shared/attributes/text', attribute: :name, url: [:account, demographic] %></td>
    <%# 🚅 super scaffolding will insert new fields above this line. %>
    <td><%= render 'shared/attributes/date_and_time', attribute: :created_at %></td>
    <td class="buttons">
      <% unless hide_actions %>
        <% if can? :edit, demographic %>
          <%= link_to t('.buttons.shorthand.edit'), [:edit, :account, demographic], class: 'button-secondary button-smaller' %>
        <% end %>
        <% if can? :destroy, demographic %>
          <%= button_to t('.buttons.shorthand.destroy'), [:account, demographic], method: :delete, data: { confirm: t('.buttons.confirmations.destroy', model_locales(demographic)) }, class: 'button-secondary button-smaller' %>
        <% end %>
        <%# 🚅 super scaffolding will insert new action model buttons above this line. %>
      <% end %>
    </td>
  </tr>
<% end %>
